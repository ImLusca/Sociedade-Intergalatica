import React from 'react';

const Reports = ({ userType }) => {
  return (
    <div>
      <h1>Relatórios para {userType}</h1>
      {/* Implementar botões ou links para diferentes relatórios */}
    </div>
  );
};

export default Reports;
